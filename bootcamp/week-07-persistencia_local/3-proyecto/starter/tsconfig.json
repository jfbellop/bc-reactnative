{
  // tsconfig.json admite comentarios (JSONC)
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    // expo/types tipa `process.env` (EXPO_PUBLIC_*) y el entorno de Metro
    "types": ["expo/types"]
  }
}